<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_2"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center">

    <!-- Timer Display -->
    <TextView
        android:id="@+id/tv_timer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Time Left: 60s"
        android:textSize="22sp"
        android:textColor="#D32F2F"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp" />

    <!-- Question Box -->
    <TextView
        android:id="@+id/tv_question"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Question will appear here"
        android:textSize="22sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:gravity="center"
        android:padding="16dp"
        android:layout_marginBottom="15dp"
        android:background="@drawable/rounded_bg_question" />

    <!-- Answer Options -->
    <RadioGroup
        android:id="@+id/radio_group_answers"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <RadioButton
            android:id="@+id/rb_option_1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Option 1"
            android:textSize="18sp"
            android:textColor="#FFFFFF"
            android:background="@drawable/rounded_bg_option"
            android:padding="12dp"
            android:layout_marginBottom="10dp" />

        <RadioButton
            android:id="@+id/rb_option_2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Option 2"
            android:textSize="18sp"
            android:textColor="#FFFFFF"
            android:background="@drawable/rounded_bg_option"
            android:padding="12dp"
            android:layout_marginBottom="10dp" />

        <RadioButton
            android:id="@+id/rb_option_3"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Option 3"
            android:textSize="18sp"
            android:textColor="#FFFFFF"
            android:background="@drawable/rounded_bg_option"
            android:padding="12dp"
            android:layout_marginBottom="10dp" />

        <RadioButton
            android:id="@+id/rb_option_4"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Option 4"
            android:textSize="18sp"
            android:textColor="#FFFFFF"
            android:background="@drawable/rounded_bg_option"
            android:padding="12dp"
            android:layout_marginBottom="10dp" />
    </RadioGroup>

    <!-- Waiting Message (Initially Hidden) -->
    <TextView
        android:id="@+id/tv_waiting_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Waiting for other players to complete..."
        android:textSize="18sp"
        android:textColor="#FFEB3B"
        android:textStyle="bold"
        android:gravity="center"
        android:padding="10dp"
        android:visibility="gone"
        android:layout_marginTop="20dp" />

    <!-- Next/Submit Button -->
    <Button
        android:id="@+id/btn_next_submit"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:text="Next"
        android:backgroundTint="#FFA500"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:enabled="false"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="20dp" />

</LinearLayout>
